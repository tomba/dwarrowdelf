<UserControl x:Class="Dwarrowdelf.Client.UI.ConcreteObjectPropertiesControl"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 mc:Ignorable="d">
	<UniformGrid Columns="2">
		<TextBlock>Name</TextBlock>
		<TextBlock Text="{Binding Name}" />

		<TextBlock>Color</TextBlock>
		<TextBlock Text="{Binding Color}" />

		<TextBlock>EffectiveColor</TextBlock>
		<TextBlock Text="{Binding EffectiveColor}" />

		<TextBlock>MaterialID</TextBlock>
		<TextBlock Text="{Binding Material.ID}" />
		
		<TextBlock>Desc</TextBlock>
		<TextBlock Text="{Binding Description}" />
	</UniformGrid>
</UserControl>
