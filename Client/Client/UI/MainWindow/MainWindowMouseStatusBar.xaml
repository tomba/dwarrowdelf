<UserControl x:Class="Dwarrowdelf.Client.UI.MainWindowMouseStatusBar"
			 x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
			 xmlns:ui="clr-namespace:Dwarrowdelf.Client.UI">
	<StatusBar DataContext="{Binding Path=(ui:MapControlDebugData.Data)}">
		<StatusBar.Resources>
			<ui:CoordinateValueConverter x:Key="conv" />
		</StatusBar.Resources>
		
		<TextBlock>RP</TextBlock>
		<TextBlock Text="{Binding RenderPoint, Converter={StaticResource conv}, FallbackValue=0.0}" />
		<Separator />

		<TextBlock>RT</TextBlock>
		<TextBlock Text="{Binding RenderTile, Converter={StaticResource conv}, FallbackValue=0.0}" />
		<Separator />

		<TextBlock>ST</TextBlock>
		<TextBlock Text="{Binding ScreenTile, Converter={StaticResource conv}, FallbackValue=0.0}" />
		<Separator />

		<TextBlock>MT</TextBlock>
		<TextBlock Text="{Binding MapTile, Converter={StaticResource conv}, FallbackValue=0.0}" />
		<Separator />

		<TextBlock>ML</TextBlock>
		<TextBlock Text="{Binding MapLocation, Converter={StaticResource conv}, FallbackValue=0.0}" />

	</StatusBar>
</UserControl>
